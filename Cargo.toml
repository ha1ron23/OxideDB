[package]
name = "oxidedb"
version = "1.0.0"
description = "A production-ready asynchronous B-Tree key-value store with RESP and WAL"
edition = "2021"

[dependencies]
tokio = { version = "1.38", features = ["full"] }
thiserror = "1.0"
tracing = "0.1"
tracing-subscriber = "0.3"
